import 'package:flutter/widgets.dart';

void registerCameraViewFactory(String viewType) {}

bool attachVideoIfAvailable() => true;

Widget buildCameraView({Key? key, required String viewType}) {
  return const SizedBox.expand();
}
